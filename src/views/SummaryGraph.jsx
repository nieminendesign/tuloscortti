// Näyttää graafin kierroksen etenemisestä (raakapisteet / nettopisteet)
import { Line } from 'react-chartjs-2';

export default function SummaryGraph({ players, scores, holes }) {
  // Luo dataa Chart.js -komponentille
}